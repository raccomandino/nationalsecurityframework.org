<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Score..</title>
    <!-- Google CSE Script -->
    <script async src="https://cse.google.com/cse.js?cx=b780383c3de6644e5"></script>
    <style>
        /* Reset and font setup */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        /* Body background and layout */
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background: linear-gradient(135deg, #4c2a84, #3bb8b4);
            color: #ffffff;
        }

        /* Main container styling */
        .container {
            text-align: center;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
            background-color: #ffffff;
            width: 90%;
            max-width: 600px;
            transition: transform 0.2s ease;
            color: #333;
        }
        .container:hover {
            transform: scale(1.02);
        }

        /* Logo styling */
        .logo img {
            max-width: 120px;
            margin-bottom: 20px;
            animation: fadeIn 1.2s ease-in-out;
            border-radius: 6px;
        }

        /* Heading and text styling */
        h1 {
            font-size: 1.8em;
            margin-bottom: 10px;
            color: #333;
        }

        p {
            font-size: 1em;
            color: #666;
            margin-bottom: 20px;
        }

        /* Search box styling */
        .gcse-search {
            margin-top: 20px;
        }

        /* Animation for logo */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Responsive adjustments */
        @media (max-width: 600px) {
            h1 {
                font-size: 1.5em;
            }

            p {
                font-size: 0.9em;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Logo section -->
        <div class="logo">
            <img src="https://raw.githubusercontent.com/raccomandino/nationalsecurityframework.org/refs/heads/main/images/Scorenet%20Logo.PNG?token=GHSAT0AAAAAACZLZ2ZPQOC7OO6LHPJMMYWMZZALJDA" alt="Your Logo" onerror="this.style.display='none'">
        </div>
        
        <!-- Heading and Description -->
        <h1>Score..</h1>
        <p>Find exactly what you’re looking for with our Score Search experience.</p>
        
        <!-- Google CSE Search Box -->
        <div class="gcse-search"></div>
    </div>

    <!-- Callback Script for Google CSE -->
    <script>
        var myWebResultsRenderedCallback = function() {
            for (var i = 0; i <= 9; i++) {
                var findMoreElement = document.getElementsByClassName("gcsc-find-more-on-google")[i];
                if (findMoreElement) {
                    findMoreElement.style.display = "none";
                }
            }
        };

        window.__gcse = window.__gcse || {};
        window.__gcse.searchCallbacks = {
            web: {
                rendered: myWebResultsRenderedCallback,
            },
        };
    </script>
</body>
</html>
